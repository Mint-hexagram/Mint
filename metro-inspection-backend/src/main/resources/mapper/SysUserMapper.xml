<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metro.inspection.mapper.SysUserMapper">

    <resultMap id="SysUserResult" type="com.metro.inspection.entity.SysUser">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="remark" column="remark"/>
        <association property="dept" javaType="com.metro.inspection.entity.SysDept">
            <id property="deptId" column="dept_id"/>
            <result property="deptName" column="dept_name"/>
        </association>
    </resultMap>

    <select id="selectUserPage" resultMap="SysUserResult">
        SELECT u.user_id, u.username, u.nickname, u.phone, u.status, u.create_time, u.remark, u.dept_id, d.dept_name
        FROM sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        <where>
            ${ew.sqlSegment}
        </where>
        ORDER BY u.create_time DESC
    </select>

    <select id="findUserPage" resultMap="SysUserResult">
        SELECT u.user_id, u.username, u.nickname, u.phone, u.status, u.create_time, u.remark, u.dept_id, d.dept_name
        FROM sys_user u
                 LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        <where>
            <if test="user.username != null and user.username != ''">
                AND u.username LIKE CONCAT('%', #{user.username}, '%')
            </if>
            <if test="user.phone != null and user.phone != ''">
                AND u.phone LIKE CONCAT('%', #{user.phone}, '%')
            </if>
            <if test="user.status != null">
                AND u.status = #{user.status}
            </if>
            <if test="user.deptId != null">
                AND (u.dept_id = #{user.deptId} OR d.ancestors LIKE CONCAT('%,', #{user.deptId}, ',%'))
            </if>
        </where>
        ORDER BY u.create_time DESC
    </select>
</mapper> 